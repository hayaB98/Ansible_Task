---
 - name: copy module
   hosts: all
   become: yes
   gather_facts: no
   vars_prompt:
    - name: path
      prompt: Enter the path where the files should be coppied
      private: false
   vars_files: ./path.yml
   tasks:
    - copy:
       src: "{{item}}"
       dest: "{{path}}"
      with_items:
       - "{{hypervisor_files}}"
       - "{{undercloud_files}}"
       - "{{overcloud_files}}"